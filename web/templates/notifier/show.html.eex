<script>
    window.options = {
        applications: ['one'],
        eventNames: ['conversion'],
        keys: ['name'],
    };

    window.notifier = <%= raw @notifier_as_json %>;
</script>

<div id="rules">
    <div id="content"></div>
</div>
<script src="<%= static_path(@conn, "/js/rules.js") %>"></script>
